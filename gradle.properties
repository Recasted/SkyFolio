# Minecraft / loader versions
# 26.1.2 ships unobfuscated - there is no Yarn mapping artifact to set here at all.
minecraft_version=26.1.2
# Confirmed against a real, currently-published 26.1.2 mod (not a guess).
loader_version=0.18.5

# Fabric API - confirmed against a real, currently-published 26.1.2 mod.
fabric_version=0.149.0+26.1.2

# Mod metadata
mod_version=0.8.0-bulkimporter
maven_group=com.example.skytracker
archives_base_name=skyblock-tracker

org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
